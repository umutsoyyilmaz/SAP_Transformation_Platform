# UI Design Index

Purpose: UI specs, visual contracts, component variants, and design-system material.

Contents:
- `UID-*` files: UI design documents
- `DESIGN-SYSTEM.md`: shared design language reference

When to use:
- Use before frontend implementation.
- Treat this folder as the source of truth for visual states and component behavior.
